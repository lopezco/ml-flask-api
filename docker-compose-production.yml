version: '3'
services:
  flask-ml:
    container_name: flask-ml-${VERSION}
    build: .
    image: flask-ml:${VERSION}
    env_file:
      - variables-production.env
    restart: always
    ports:
      - "${PORT_EXTERNAL}:${PORT}"
